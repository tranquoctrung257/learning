#include <bits/stdc++.h>

using namespace std;

int main(){
    long long x; 
    cin >> x;
    cout << pow(x,3) + 3*pow(x,2) + x + 1 << endl;
    // hoặc
    // long long kq = x * x * x + 3 * x * x + x + 1;
}