# VÒNG LẶP (LOOP)

## Đặt vấn đề
**Bài toán:** Hãy viết chương trình in ra 1000 dòng "Hello World!".
Nếu sử dụng 1000 dòng lệnh `cout`, điều này tốn rất nhiều thời gian và là **KHÔNG KHẢ THI**. 
Giải pháp tối ưu cho những tác vụ lặp đi lặp lại như vậy chính là sử dụng **VÒNG LẶP**.

## 1. VÒNG LẶP FOR

### Cú pháp
```cpp
for ([Câu lệnh khởi tạo]; [Điều kiện lặp]; [Câu lệnh cập nhật]) {
    // Code bên trong vòng lặp for
}
```

### Thành phần
Vòng lặp `for` gồm 3 phần chính, các phần được phân cách nhau bằng dấu chấm phẩy `;`:
*   **Phần 1 - Câu lệnh khởi tạo:** Thường là khai báo và khởi tạo một biến có nhiệm vụ làm biến đếm cho vòng for (ví dụ: `i`, `j`, `k`).
*   **Phần 2 - Điều kiện lặp:** Chừng nào giá trị của điều kiện lặp còn có giá trị `true` thì vòng for còn tiếp tục chạy.
*   **Phần 3 - Cập nhật biến đếm:** Thường phần này sẽ thay đổi giá trị của biến đếm sau khi các câu lệnh ở bên trong vòng lặp for được thực hiện xong.

```cpp
#include <bits/stdc++.h>

using namespace std;

int main(){
    // for (khởi tạo biến đếm; điều kiện lăp;giá trị mà biến i tăng thêm sau mỗi vòng lặp)
    // for(int i = 1; i <= 10 ; i++){
    //     cout << i << " Xin chào!" << endl;
    // }

    // các bước chạy
    // b1 khởi tạo biến i = 1
    // sau đó nhảy sang i <= 10 để check điều kiện 
    // nếu đúng thì xuống sẽ chạy code trong vòng for luôn 
    // sau khi chạy xong sẽ lên dòng i++ để thay đổi giá trị i (tăng nó lên 1 đơn vị)
    // tăng xong sẽ chạy i <= 10 để check đk nếu đúng sẽ chạy các dòng code bên dưới 
    // chạy xong thì sẽ quay lại dòng i++ rồi quay lại check đk 
    // xong code chạy như vậy đến khi đk không đúng nữa thì dừng vòng for 
    // lưu ý i = 1 chỉ thực hiện đúng 1 lần duy nhất ở vòng for đầu tiên thôi 

    // trong dấu {} của vòng for gọi là enclosing scope phạm vi trong đóng mở ngoặc nhọn và i chỉ được chạy trong phạm vi này.
    // nếu mà ra ngoài in ra biến trong enclosing scope thì chương trình sẽ không biết biến đó là gì
    // còn trong hàm main thì gọi là local
    // còn phạm vi lớn nhất là global 

    // int a = 100; biến này thuộc phạm vị local

    // quy tắc tìm biến sẽ tìm từ trong ra ngoài

    // int i, dem = 0;
    // for(i = 0;i<5;i++){
    //     ++dem;
    // }
    // cout << "gia tri cua bien dem la: " << dem << endl;
    // cout << "gia tri cua bien dem la: " << i << endl;
    // vòng for thực hiện 10 lần 0,1,2,3,4..9
    // còn i sẽ in ra giá trị vi phạm lần đầu là 10 | tăng lên 10 và check đk sai thoát vòng for luôn 
    // for(i = 0;i<=5;++i){
    //     ++dem;
    // }
    // cout << "gia tri cua bien dem la: " << dem << endl;
    // cout << "gia tri cua bien dem la: " << i << endl;

    // giá trị biến đếm là 6 lần vì vòng for lặp 
    // còn giá trị của biến i là 5 
    // ++i với i++ không khác gì nhau
    // ++i sẽ nhanh hơn 1 tí với i++  
    // dem++ hay ++dem thì chương trình này đều như nhau 
}

```

```cpp
#include <iostream>
using namespace std; 
int main(){
    // chương trình này không có điều kiện dừng nên chương trình nó sẽ lặp vĩnh viễn
    // for(int i;;i+=2){
    //     cout << i << endl;
    // }

    // trong c++ các giá trị khác 0 được coi là đúng nên 5 là đúng nên code cũng chả khác gì vòng for ở trên
    // for(int i;5;i+=2){
    //     cout << i << endl;
    // }
    
    // for(int i;0;i+=2){
    //     cout << i << endl;
    // }
    // chương trình này sẽ không chạy vì khởi tạo biến i xong check đk false thì dừng luôn 

}

```

```cpp
#include <iostream>
using namespace std;

int main(){
    for(int i = 1;i<=0;i+=1){
        cout << i << endl;
    }
    // chương trình này cũng sẽ không chạy vì i<= 0 là false nên cũng sẽ không chạy
    // cout << (1 <= 0) << endl; trả về false ngay từ đầu

    // ví dụ chương trình này
    for(int i = 1;i<= 5 && i % 2 == 0;i++){
        cout << i << endl;
    }
    // vòng for này sẽ dừng ngay từ lúc đầu luôn vì i = 1 check thêm 1 % 2 == 0 ==> sai nên chương trình sẽ dừng và không chạy
}

```

### Cách sử dụng
Vòng lặp `for` được sử dụng nhiều nhất, và thường được sử dụng khi **biết trước số vòng lặp** cần thực hiện. 
*Lưu ý:* Vòng `for` không nhất thiết phải có đủ cả 3 phần, nhưng **phải luôn có đủ 2 dấu `;`** để phân cách giữa 3 phần.

### Ví dụ
```cpp
#include <bits/stdc++.h>

    // for (khởi tạo biến đếm; điều kiện lăp;giá trị mà biến i tăng thêm sau mỗi vòng lặp)
    // for(int i = 1; i <= 10 ; i++){
    //     cout << i << " Xin chào!" << endl;
    // }

    // các bước chạy
    // b1 khởi tạo biến i = 1
    // sau đó nhảy sang i <= 10 để check điều kiện 
    // nếu đúng thì xuống sẽ chạy code trong vòng for luôn 
    // sau khi chạy xong sẽ lên dòng i++ để thay đổi giá trị i (tăng nó lên 1 đơn vị)
    // tăng xong sẽ chạy i <= 10 để check đk nếu đúng sẽ chạy các dòng code bên dưới 
    // chạy xong thì sẽ quay lại dòng i++ rồi quay lại check đk 
    // xong code chạy như vậy đến khi đk không đúng nữa thì dừng vòng for 
    // lưu ý i = 1 chỉ thực hiện đúng 1 lần duy nhất ở vòng for đầu tiên thôi 
```



### Lệnh Break và Continue
*   **Lệnh `break`:** Khi muốn dừng vòng lặp for với một điều kiện cho trước ta dùng câu lệnh `break`. Khi gặp `break`, vòng for sẽ kết thúc ngay lập tức.
    ```cpp
    for ([Câu lệnh khởi tạo]; [Điều kiện lặp] ; [Câu lệnh cập nhật]) {
        // Các câu lệnh trước break
        if (condition) {
            break; // Thực thi khi condition là true, thoát khỏi vòng lặp
        }
        // Nhóm câu lệnh sau break
    }
    ```
*   **Lệnh `continue`:** Khi muốn bỏ qua phần còn lại của vòng lặp hiện tại và chuyển sang vòng lặp (iteration) mới luôn. Nhóm câu lệnh đứng sau `continue` sẽ không được thực hiện trong lần lặp đó.
    ```cpp
    for ([Câu lệnh khởi tạo]; [Điều kiện lặp] ; [Câu lệnh cập nhật]) {
        // Các câu lệnh trước continue
        if (condition) {
            continue; // Bỏ qua các lệnh phía dưới, tiến hành cập nhật và lặp vòng mới
        }
        // Nhóm câu lệnh sau continue (không được chạy nếu gặp continue)
    }
    ```
```cpp
#include <iostream>
using namespace std;

int main(){
    // break để kết thúc vòng lặp ngay ở lần lặp nào đó
    for(int i = 1;i<= 10;i++){
        cout << i << endl;
        if(i == 6) break; // i bằng 6 sẽ break và sẽ không chạy những vòng lặp tiếp theo
    }

    for(int i = 1;i<=10;i++){
        cout << i << endl;
        continue; // vòng for gặp câu lệnh continue thì sẽ bỏ qua các câu lệnh bên dưới. để quay trở lại câu lệnh mới.
        cout << "hello"<< endl;
    }
}

```

### Các ví dụ về vòng lặp

```cpp

#include <iostream>
using namespace std;

int main(){
    // ví dụ chương trình tính tổng các số chẵn 2 4 6 8 ,...n
    n = 10;
    for(int i = 1;i<=n;i++){
        if(i%2==0) tong+=i;
    }
    cout << tong << endl;
    
    // hoặc có cách 2
    for(int i = 2;i<=n;i+=2){
        tong+=i;
    }
    cout << tong << endl;
}
```

```cpp
#include <iostream>
using namespace std;

int main(){
    // ví dụ chương trình in ra các bội số của n 
    n = 16;
    for(int i = 0;i<=n;i++){
        if(i%3==0) cout << i << endl;
    }
}
```
```cpp

#include <iostream>
using namespace std;

int main(){
    // để viết phương trình tính tổng 1 số nào đó bằng vòng for thì đầu tiên phải khởi tạo biến đó = 0
    int n,tong = 0;
    // trong đây sẽ có 2 lỗi sai 
    // lỗi thứ nhất là không khởi tạo biến tong = 0 mà chỉ khai báo nó thôi nên nó sẽ trả về giá trị rác nào đó.
    //  còn lỗi thứ 2 là cout tong ra ở trong vòng for luôn như thế là sai mỗi vòng for chạy nó sẽ in ra luôn hết tất cả các giá trị cộng cho từng vòng for
    cin >> n;
    for(int i = 1;i <= n;i++){
        tong+=i;
    }
    cout << tong << endl;
}
```

```cpp
#include <iostream>
using namespace std;

int main(){
        // vòng for lồng nhau
    for(int i = 1; i<=5;i++){
        for(int j = 1;j <= 3;j++)
        cout << i << " " << j << endl;
    }
}
```
---

## 2. VÒNG LẶP WHILE

### Cú pháp
```cpp
while ([Điều kiện lặp]) {
    // Các câu lệnh của vòng lặp
}
```

### Cách sử dụng
*   **Hoạt động:** Khi điều kiện lặp còn đúng thì vòng while còn tiếp tục hoạt động và các câu lệnh bên trong thân vòng lặp sẽ tiếp tục được thực hiện.
*   **Sử dụng khi nào:** Vòng lặp `while` được sử dụng khi **chưa xác định được** số vòng lặp cần thực hiện.
*   **Lỗi thường gặp:** Vòng lặp bị lặp vĩnh viễn (vô hạn) do không có câu lệnh cập nhật làm sai điều kiện lặp.

### Ví dụ

```cpp

#include <iostream>
using namespace std;

int main(){
    // vòng lặp while 
    // while (điều kiện lặp)
    // {
    //     /* code */
    // }
}

```

```cpp

#include <iostream>
using namespace std;

int main(){
    n = 5;
    while (n >= 0){
        cout << n << endl;
        --n;
    }
}

```

```cpp

#include <iostream>
using namespace std;

int main(){
    // trong vòng while có thể bỏ break continue bình thường
    // ví dụ in các số tự 1 đến n 
    n = 10,i = 1;
    while (i <= n)
    {
        cout << i << endl;
        i++;
    }
}
```
```cpp
#include <iostream>
using namespace std;

int main(){
    // ví dụ áp dụng vòng lặp while để đếm số chữ số của 1 số.
    n = 13233132;
    int dem = 0;
    while (n !=0 ){
        ++dem;
        n /=10;
    }
    cout << dem << endl;
}
```

```cpp
#include <iostream>
using namespace std;

int main(){
    
    // ví dụ code tính tổng từng chữ số của 1 số
    n = 13233132;
    int ton = 0;
    while (n !=0 ){
        ton += n%10;
        n /=10;
    }
    cout << ton << endl;
}

```
> vòng for áp dụng khi biết trước số lần lặp
> vòng while áp dụng khi chưa biết số lần lặp chính xác
---

## 3. VÒNG LẶP DO WHILE

### Cú pháp
```cpp
do {
    // Code trong vòng lặp
} while ([Điều kiện lặp]);
```

### Cách sử dụng
*   **Hoạt động:** Tương tự như vòng lặp `while` nhưng có một chút sự khác biệt: vòng lặp `do while` **luôn thực thi code bên trong vòng lặp 1 lần đầu tiên**, sau đó mới kiểm tra điều kiện lặp.
*   **Sử dụng khi nào:** Cũng được sử dụng khi chưa xác định được số vòng lặp cần thực hiện, nhưng bạn cần khối lệnh thực thi ít nhất một lần.
*   **Lỗi thường gặp:** Thiếu dấu chấm phẩy `;` ở đằng sau `while(...)`.

### Ví dụ
| Code | Output | Giải thích |
| :--- | :--- | :--- |
| `int i = 100;`<br>`do {`<br>&nbsp;&nbsp;`cout << i << endl;`<br>`} while(i < 100);` | 100 | Mặc dù `i < 100` là sai ngay từ đầu, vòng lặp vẫn chạy lần đầu tiên và in ra 100. |
| `int i = 1;`<br>`do {`<br>&nbsp;&nbsp;`cout << i << endl;`<br>&nbsp;&nbsp;`i++;` *(Cần thêm lệnh này để giống output)* <br>`} while(i <= 5);` | 1<br>2<br>3<br>4<br>5 | Chạy giống while thông thường nếu điều kiện đúng |